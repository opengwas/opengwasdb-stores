# Workflow profiles

Server-specific resource defaults for the Phase A workflow (cores, memory,
job limits). Kept out of `workflow/Snakefile` so that the DAG stays a
statement about dependencies and the machine stays a statement about this
machine.

Empty until Phase A is implemented.
